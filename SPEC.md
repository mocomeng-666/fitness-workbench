# 个人健身训练工作台 — 开发规格书（施工版）

> 公开文字版说明：动作详情以文字要领为准；身高由用户填写；不预设个人训练时段。历史版本记录仅供参考，当前行为以源码和 README 为准。核心训练功能离线可用，AI 识别需要联网。
> 依据：《个人健身训练工作台_PRD_v1.0.docx》+ 与产品所有者的 8 项确认决策。
> 本文件是唯一施工依据；与 PRD 冲突处以本文件「已确认决策」为准。

## 0. 已确认决策（优先级最高）

1. 健身房器械按**齐全**处理，PRD 动作表原样采用。
2. 加重梯度统一 **2.5kg**（上肢/下肢相同）；自重动作进阶 = 先加次数/保持时长。
3. 训练时段由用户自行安排；**标准 60 分钟为默认**，25 分钟精简模式为状态差时降级。
4. 部署：免费静态托管（Cloudflare Pages），PWA 可安装、离线可用。
5. 代码形态：**多文件开发，构建为单文件交付**（dist/index.html 内联 css/js；sw.js 与 manifest 保持独立文件）。
6. 训练开始日期：首次配置时**手动选择**第 1 周周一；设置中允许重启周期并重选。
7. 体态照片：主存 IndexedDB；**不进 JSON 备份**；单独「导出照片 ZIP」，每 4 周首页轻提醒一次。
8. UI：浅色主题，**浅绿主色 + 黄色点缀**（黄色仅用于积极反馈/轻提醒，不用大段文字）；安全警告（膝部、红旗）用标准红/橙语义色。

## 1. 产品定位与硬边界

- 单用户、无账户、无登录、无云同步、无服务器；全部数据在浏览器 IndexedDB。
- 语言中文；重量单位 kg（允许 1 位小数）。
- **不做**：健身房外活动记录、饮食、心率/穿戴设备、社交、医疗诊断、BMI 驱动的减脂建议、热量估算激励。
- 所有训练建议在 UI 上必须标注为「建议」而非诊断；文案用「改善控制、改善体态」，禁止治疗性表述。

## 2. 技术架构

- 纯前端：原生 HTML/CSS/JS（不引入框架与构建依赖，保持可维护与可单文件化）。
- 本地数据库：**IndexedDB**（封装 Promise 版），数据库名 `fitness-workbench`，版本 1。
- PWA：`manifest.webmanifest`（浅绿主题色）+ `sw.js`（Cache First：应用外壳；安装后核心训练功能可离线使用；AI 识别需联网）。
- 构建：`build.py` 把 `styles.css` 与全部 js 按顺序内联进 `dist/index.html`；`dist/` 另含 `sw.js`、`manifest.webmanifest`。
- 目标视口：360px 宽可完整操作；桌面端居中限宽容器（max-width ~480px 的 App 列 + 宽屏时适度放宽）。

### 文件结构

```
fitness-workbench/
  SPEC.md            本文件
  src/
    index.html       应用外壳（含五个导航页容器）
    styles.css       全部样式（CSS 变量定义配色）
    data.js          8周计划、动作库、文案常量（PROGRAM / EXERCISES / COPY）
    db.js            IndexedDB 封装 + 导入导出（JSON / ZIP）
    engine.js        规则引擎（进阶、当日减量、膝部、周报计算）
    app.js           路由 + 五个页面渲染 + 事件
    sw.js            Service Worker
    manifest.webmanifest
  build.py           单文件构建脚本
  dist/              构建产物
```

## 3. 配色（CSS 变量）

- `--green-primary: #7FB069`（浅绿主色，可按观感微调至同色系）
- `--green-dark: #5A8F4A`（主按钮文字/强调）
- `--green-bg: #F2F8EF`（浅绿底色块）
- `--yellow-accent: #F2C94C`（仅积极反馈/轻提醒徽章）
- `--warn-orange: #E8890C`、`--danger-red: #D64545`（膝部警告/红旗）
- 背景 `#FAFAF7`，卡片白，正文 `#2B2B2B`，次级 `#6B6B6B`。
- 底部 5 Tab 导航（移动端固定底部）。

## 4. 数据模型（IndexedDB object stores）

| store | keyPath | 说明 |
|---|---|---|
| profile | "me" | birthYear/sex/heightCm/开始日期 chosenStartDate/每周日程确认/创建时间 |
| sessions | id (uuid) | {id, date, templateId, weekIndex, dayType, mode(standard/short), status(未开始/进行中/已完成/部分完成/已跳过), readiness:{sleepHours,energy1to5,fatigue1to5,kneePain0to10,note}, exercises:[{exerciseId,sets:[{setIndex,plannedReps,weightKg,reps,rir,painNote}],substituted}], postFeedback:{difficulty1to5,kneeChange,completed}, createdAt, updatedAt} |
| measurements | id | {id, date, weightKg, waistCm?, hipCm?, thighCm?} |
| photos | id | {id, date, view(front/side/back), blob, createdAt}（仅存 IndexedDB） |
| settings | "app" | {restTimerDefault, weightIncrement:2.5, lastPhotoExportAt, lastBackupAt, schemaVersion:"1.0"} |

训练模板与 8 周计划是**代码内配置**（data.js），不入库；Session 记录实际执行。

## 5. 训练内容配置（data.js 必须忠实实现）

### 每周日程（默认）
- 周一 力量A（约50–60min，中等）；周二 椭圆仪＋体态/自重（30–40min，轻）；周三 力量B；周四 跑步机快走＋体态/核心；周五 力量A/B 交替；周末休息（产品内不记录周末活动）。
- 交替：第1周 A-B-A，第2周 B-A-B，后续重复。

### 周期阶段
- 第1–2周：所有动作 2 组，RIR 3（动作与耐受）
- 第3–4周：前四个主要动作增至 3 组（建立训练量）
- 第5–7周：主要动作 3 组，辅助动作 2–3 组（稳定进步）
- 第8周：全部减为 2 组或降重（恢复与复盘）

### 力量A
| 动作 | 类型 | 次数 | 1–4周组数 | 目标RIR | 休息 |
|---|---|---|---|---|---|
| 哑铃高脚杯箱式深蹲 | 自由重量 | 8–10 | 2→3 | 3→2–3 | 90s |
| 哑铃罗马尼亚硬拉 | 自由重量 | 8–10 | 2→3 | 3→2–3 | 90s |
| 坐姿划船机 | 固定器械 | 10–12 | 2→3 | 3→2–3 | 75–90s |
| 坐姿推胸机 | 固定器械 | 8–12 | 2→3 | 3→2–3 | 75–90s |
| 自重臀桥 | 自重 | 10–15 | 2 | 3 | 60s |
| 死虫 | 自重 | 每侧6–10 | 2 | 3 | 45–60s |

### 力量B
| 动作 | 类型 | 次数 | 组数 | 目标RIR | 休息 |
|---|---|---|---|---|---|
| 有支撑分腿蹲 | 自重→哑铃 | 每侧6–8 | 2→3 | 3→2–3 | 90s |
| 坐姿腿弯举 | 固定器械 | 10–12 | 2→3 | 3→2–3 | 75s |
| 上斜哑铃卧推 | 自由重量 | 8–10 | 2→3 | 3→2–3 | 90s |
| 中立握高位下拉 | 固定器械 | 8–12 | 2→3 | 3→2–3 | 75–90s |
| 绳索后束划船/反向飞鸟 | 绳索 | 12–15 | 2→2–3 | 3 | 60s |
| 侧桥 | 自重 | 每侧15–30秒 | 2 | 3 | 45–60s |

### 周二（椭圆仪＋体态/自重）
椭圆仪 20–30min 中等强度（能说完整句子但不能唱歌）；上斜肩胛俯卧撑 2×8–12；自重臀桥 2×12；死虫每侧 2×6–8；自重提踵 2×12–15。

### 周四（跑步机＋体态/核心）
跑步机快走 20–30min（坡度 0–3%，不扶扶手）；上斜俯卧撑 2×8–12；侧桥每侧 2 组；徒手髋铰链 2×10；胸椎旋转每侧 2×6。明显疲劳时允许把周四设为休息日（界面上提供「今天休息」）。

### 体态小训练（8–10min，周二/周四内含，也可单独入口）
仰卧或靠墙下巴回收 2×8（每次保持 3–5s）；胸椎伸展 1×6–8；上斜肩胛俯卧撑 2×8–12；靠墙滑手 2×8；死虫每侧 1×6。

### 有氧器械顺序
椭圆仪=前4周首选；跑步机=快走为主平地/小坡度；登山机=第5周后可选、膝部稳定时 5–10min 起步。

## 6. 规则引擎（engine.js）

### RIR
枚举 0/1/2/3/4/5+；首次使用弹说明（一组结束时估计还能规范完成的次数）。

### 双重进阶（每次训练后为下次同动作生成建议）
- 同一动作**连续两次**达到次数上限且 RIR≥2 → 建议下次 **+2.5kg**（每次只改一个变量）。
- 达到目标次数但 RIR=0 → 保持当前重量。
- 未达到次数下限、或状态差 → 降重 2.5kg、或减少 1 组、或替换。
- 自重动作：达上限且 RIR≥3 → 先加次数/保持时长（在建议文案中体现）。

### 当日减量（练前状态提交后生成建议卡，用户可接受/忽略；忽略要记录）
- 睡眠 <6h → 建议主要动作保持重量、减 1 组。
- 精力 1–2 或疲劳 4–5 → 优先提供 25 分钟精简模式。
- 明显全身酸痛 → 不加重，可缩减辅助动作。
- 膝部疼痛 ≥3 → 下肢动作不自动进阶，建议替换/缩幅。

### 膝部安全
- 箱式深蹲/分腿蹲以无痛幅度为准，「大腿平行地面」不是成功条件；膝随脚尖方向；分腿蹲允许单手支撑。
- 动作中疼痛 ≥3 或逐组增加 → 建议减重/缩幅/替换。
- **红旗症状**（卡锁、明显肿胀、无法完全伸直、反复打软腿、持续加重）→ 醒目提示：停止下肢自动进阶，建议就医/运动医学评估。红旗入口出现在练后反馈与动作异常记录中。

### 边界
建议全部标注「建议」；不因 BMI/单日体重建议减脂（减脂始终次要目标，首页不做体重焦虑式展示）。

## 7. 页面与功能（app.js）

底部 5 Tab：**今天 / 计划 / 进展 / 动作库 / 设置**。

### 首次使用流程（无 profile 时进入）
欢迎页（非医疗产品声明 + 数据仅存本地说明）→ 确认/修改档案（身高自行填写、体重、出生年、训练时间 用户自行安排、目标）→ 确认默认每周日程 → **手动选择第 1 周周一的开始日期** → 记录初始体重/可选围度/可选体态照片 → 进入首页。不要求输入起始重量（首次做动作时 RIR 校准）。

### 今天（FR-01～FR-05, FR-40）
- 显示日期、训练名称、预计用时、上次表现摘要；**两次点击内可开始训练**（AC-02）。
- 练前状态：睡眠小时（可小数）、精力 1–5、疲劳 1–5、膝部疼痛 0–10、明显酸痛开关。
- 建议卡：按规则显示「按计划 / 减一组 / 精简模式 / 上肢替代」，可接受或忽略（忽略记录）。
- 执行页：动作卡（名称/类型/计划组次/目标RIR/休息秒数）；逐组记录重量/次数/RIR/疼痛备注；新增组默认复制上一组；保存组后可启休息倒计时（默认按动作休息秒数，结束震动提醒）；动作替换（同模式同训练目标）；动作级异常反馈。
- 练后：整体难度 1–5、膝部变化、完成状态（完成/部分/跳过）；生成本次摘要 + 下次建议。
- 有氧日：器械（仅跑步机/椭圆仪/登山机）、持续时间、主观强度必填；速度/坡度/阻力选填；强度提示用说话测试（无心率设备）。**不出现任何健身房外活动选项**。

### 计划
8 周日历（当前周高亮、每天类型/用时/强度）；力量A/B 动作表；周期阶段说明；体态小训练内容。

### 进展（P1）
- 周报（8.3 节 7 项）：计划 vs 实际（标准/部分/跳过分别统计）、A/B 各动作最近表现与变化、满足加重/保持/减量的动作列表+触发原因、有氧总分钟与器械分布、膝部疼痛趋势与红旗触发、睡眠/精力与完成率的观察性描述（不推断因果）、下周建议最多 3 项。
- 趋势：训练次数、完成率、各主动作重量曲线、体重曲线。
- 身体数据录入：体重/腰围/臀围/大腿围；体重建议每周 3–7 次取周均值展示。
- 体态照片：正面/侧面/背面拍照或相册导入；同视角 4 周对比查看。

### 动作库（FR-60～63）
仅计划内动作 + 替代动作；按名称/训练日/器械检索；详情含中文名、英文名、动作要领、常见错误、文字动作说明；文字内容离线可用。

### 设置
本地数据说明；导出完整 JSON（含 schemaVersion/exportedAt/sourceDevice/profile/sessions/measurements/weeklyReviews）；CSV 导出（训练组/有氧/测量分别导出）；导入 JSON（校验 schemaVersion 与结构，冲突不合并、覆盖前确认）；导出照片 ZIP；清空数据（二次确认）；重量梯度显示 2.5kg；重新开始周期（重选开始日期）。

## 8. 非功能要求

- 360px 宽可完整操作；安装后断网可用（AC-03 飞行模式完成一节训练）；
- 首页本地加载 <1s（普通手机）；每次保存用 IndexedDB 事务；
- 文字/背景对比清晰（黄色不做正文色）；按钮 ≥44px 触控区；
- 无注册/登录/同步代码（AC-09）。

## 9. 验收锚点（实现后逐条自测）

AC-01 免注册建立档案；AC-02 两次点击进训练；AC-03 断网完成训练；AC-04 连续两次达标→加重建议；AC-05 睡眠<6h/精力≤2→减量或精简建议；AC-06 膝痛≥3→下肢不进阶；AC-07 全产品无健身房外活动记录项；AC-08 JSON 导出→导入恢复；AC-09 无登录/同步；AC-10 动作文字要领离线可读。

## 10. 动作说明

全部动作使用中文要领与常见错误说明，离线可读。

## A. 首次配置（onboarding）
- A1 身高：不预填，由用户自行填写。
- A2 出生年份：纯数字键盘输入（inputmode=numeric），无步进箭头；校验 4 位、范围 1940–2015。
- A3 首要健身目标（单选），问题"你首要的健身目标是什么？"，选项 5 项：减脂 / 增肌增力 / 改善体态 / 提升精力 / 恢复运动习惯。
- A4 每周排期：唯一写死=每周 5 个训练日。系统按「训练基础+首要目标+膝部情况」生成建议排期：
  减脂→2力量+3有氧（一三五有氧、二四力量）；增肌增力→4力量+1有氧（一二四五力量、三有氧）；
  改善体态→3力量+2有氧（一三五力量、二四有氧）；提升精力→2力量+3有氧；恢复运动习惯→3力量+2有氧。
  建议可逐天手动调整：哪 5 天训练（周一~周日任选，含周末）、每天力量或有氧；不设硬限制，偏离建议时给一句可忽略的轻提示。计划页可再改。
- A5 开始日期：维持手动选择第 1 周周一。

## B. 视觉刷新（奶油色系）
- B6 配色：今天/力量=抹茶绿 #7FB069；计划/有氧=雾蓝 #7AA5C4；进展=蜂蜜黄 #E8B84B；动作库/体态=蜜桃橘 #E8925C；设置/休息=暖灰 #9C9A94；安全警告=深橙红（明度与蜜桃橘拉开）。大面积白/奶油底，彩色只做识别。
- B7 减字：动作要领、规则解释默认折叠；建议卡只显示结论+一句原因（详细可展开）；表格能改卡片改卡片。
- B8 Emoji 点缀：导航 🏋️🗓️📈📖⚙️；训练类型 💪力量/🚶有氧/🧘体态/😴休息；状态 ✅🔥⭐🛑；正文与数据里不放。
- B9 日历重做：表格→卡片式周视图。顶部 8 周进度条+当前周徽章；7 天圆角色块卡（类型色+emoji+一句话），今天放大高亮，完成盖 ✅，休息日灰显；上下滑切周。

## C. 仪式感与文案
- C10 无推送、无红点、休息日不催；完成 ✅ 动效、🔥连续天数、第 8 周"毕业卡"。只用完成率维度，不碰热量/体重激励。
- C11 文案语气"懂你的朋友"（例："昨晚没睡好吧，今天咱们轻松点，25 分钟搞定"），集中在 copy 常量管理。

## D. 周报分享卡
- D12 进展页周报增加"生成分享卡"：浏览器 Canvas 本地生成 1080×1350 PNG，长按保存/系统分享。内容=本周完成天数、🔥连续记录、力量进步、一句鼓励。默认不放体重、不放体态照片。鼓励语=15–20 句手写语料，按当周表现分桶匹配（全勤/多数完成3-4天/少量1-2天/断周回归），桶内随机。无网络、无第三方依赖。

## E. 联动适配与暂不做
- E13 进展页周完成率分母按实际排期计算；力量日 A/B 交替跟随自定义排期；周报统计口径同步适配。
- E14 暂不做：8 周首末体态照片对比卡（阶段写真），V1.1 之后候选。

---

# V1.2 改造定稿（2026-09-04 用户确认，优先级高于上文冲突项）

背景：用户通读《通用个性化健身训练工作台_PRD_v1.0.docx》后拍板——**以当前工作台为准，唯一采纳其 A1 项（放开每周训练天数）**；通用版其余差异（多档案、安全筛查分级、计划生成引擎、7 项目标体系、器械过滤等）一律不采纳，通用版 PRD 文档暂不回改，仅作远期参考。

## A1（V1.2）每周训练天数 2/3/4/5 可选
- onboarding 顺序：选目标 → **选天数（2/3/4/5，默认 5）** → 看建议排期（可逐天微调）。
- 唯一规则改为：训练日数恰好 = 所选天数；不设其他硬限制，偏离建议给轻提示。
- 建议映射（目标 × 天数）：
  - 减脂 / 提升精力：2d=1力+1有氧（一力四有氧），3d=1力+2有氧，4d=2力+2有氧，5d=2力+3有氧
  - 增肌增力：2d=2力，3d=3力，4d=3力+1有氧，5d=4力+1有氧
  - 改善体态 / 恢复运动习惯：2d=2力，3d=2力+1有氧，4d=3力+1有氧，5d=3力+2有氧
  - 原则：天数越少力量占比越高（力量保本）；天数变化只调有氧；力量日不连续超 2 天。
- 计划页排期编辑可加天数切换；改天数 = 按当前目标重新生成建议，需 confirm 覆盖自定义。
- 数据结构不变：不存 days 字段，天数 = schedule 非 rest 数量；周完成率/周报/分享卡分母动态跟随（本就如此）；旧档案零影响（默认仍 5 天）。

---

# 构建与部署

运行 `python3 build.py`，将 `dist/` 上传至自行选择的 HTTPS 静态托管服务。每次更新应修改缓存版本。

---

# V1.3 改造定稿（2026-09-08 用户确认；优先级高于上文冲突项）

执行范围（用户拍板）：**本轮只做代码修改（A→D）+ 验证；PRD 文档同步与 Cloudflare 线上部署延后，待用户预览确认效果后另行执行。**

## A. 视觉刷新「克制、高级、生活感」（参考图：米色生活杂志风，大量留白）

### A1 CSS 变量全量替换（styles.css --root 与全局引用）
| 新变量 | 值 | 替代旧变量 |
|---|---|---|
| --bg | #F5F0E6 米色 | --bg 白 → 米 |
| --card | #FCFAF3 | --card |
| --cream | #FFFDF8 | 高亮卡/弹层内底 |
| --ink | #3A362C 暖墨 | --text #2B2B2B |
| --ink-sub | #8B8577 | --text-sub |
| --main / --main-dark | #7FB069 / #5A8F4A（不变） | 主色按钮/选中/进度 |
| --accent / --accent-soft | #6F5F9E / 底 #EFEBF5 | 深紫强调：成果区/提醒/关键数字/周报头部小标 |
| --warn / --danger | #C2410C / #B91C1C（不变） | 安全语义 |
| 类型浅底 | 力量 #E9F0E2 / 有氧 #E3EBF0 / 体态 #F4E6DA / 休息 #ECEAE4 | --bg-strength 等（微调） |
| 类型深字 | 力量 #5A8F4A / 有氧 #557E9E / 体态 #B96A38 / 休息 #6F6D68 | 徽章文字 |
- **蜂蜜黄 #E8B84B 与五模块页面色（hdr-plan 雾蓝 / hdr-progress 蜂蜜黄 / hdr-library 蜜桃橘 / hdr-settings 暖灰 / hdr-today 绿做页头整块色）全部退场**：顶栏不再随模块变色，一律 --bg 米底。
- 导航激活态：抹茶绿加粗 + 文字前小圆点（可用 ::before），不再用色块。
- 圆角：卡片 14→20px，按钮 12px，徽章 10px；卡间距 20–24px；卡片内边距 ≥20px。
- 阴影：一律 0 1px 2px rgba(0,0,0,0.04)（近无），层次靠底色差（卡 --card 浮于 --bg）。
- 字体：无衬线不变（可读性优先），大标题/页头字号保持但字重 600–700、letter-spacing .02em。
- 次级/说明文字统一 --ink-sub。正文/数据不用黄、不用紫（紫仅 --accent 用途）。emoji 全保留不动，视觉后由用户审。

### A2 分享卡（app.js canvas 绘制常量）配色同步
底 #FCFAF3；顶部条 #7FB069 抹茶绿白字「本周训练小结」；大数字用绿 #5A8F4A；🔥 连续用 --accent 深紫 #6F5F9E（替代原金黄 #A87F1E）；鼓励语 --ink；页脚 --ink-sub。布局不变。

## B. Onboarding 简化
- B1 不收集或预设固定训练时段。
- B2 删除排期编辑器整卡（7 天三态、ob-sched、ob-count 计数、ob-deviation 偏离提示、恰好 N 校验、切目标/切天数重生成 UI 逻辑全移除）。**保留**「每周计划练几天 2/3/4/5」步骤（只选天数，不再展示排期）。
- B3 保存时静默：profile.schedule = suggestSchedule(primaryGoal, days)（确定性函数，不再暴露编辑器）。
- B4 「开始日期」卡下方固定小字：我们已按你的目标生成初始课表，稍后可在「计划」页调整。

## C. 身体情况三档单选（替换原「膝部情况」；训练基础步骤保留不动）
- C1 onboarding 身体卡：标题「有没有需要特别照顾的身体部位？」单选三档 radio-chip，**语义写进选项副标小字**（chip 下 12px --ink-sub）：
  - none 没受伤 —— 副标：没有需要留意的旧伤或不适
  - knee 膝部有伤 —— 副标：膝盖有旧伤或不适，勾选后自动避开伤膝动作
  - other 其他部位有伤 —— 副标：肩/腰/腕等别处有不适，训练时给你相应提示
  默认 none。不另设独立提示行。
- C2 profile 新字段 bodyStatus:'none'|'knee'|'other'。迁移：旧 kneeNote:'left'→'knee'；'none'/缺失→'none'。
- C3 动作部位标注（data.js EXERCISES 每动作可选 notes:[] 部位数组，knee 语义 = 原 kneeCaution:true，统一迁入 notes 并保留兼容读法）：
  - knee：哑铃高脚杯箱式深蹲、有支撑分腿蹲、（有氧器械）登山机
  - shoulder：上斜哑铃卧推、坐姿推胸机、上斜俯卧撑、绳索后束划船/反向飞鸟
  - back：哑铃罗马尼亚硬拉、坐姿划船机、中立握高位下拉
  - 其余动作不标（克制，宁少勿多）。体态小训练动作不标。
- C4 编排规避（bodyStatus==='knee'）：力量模板解析出的动作若带 knee 标注 → 自动替换为 SUBSTITUTES 中「该模板当日尚未包含」的首个替代动作（如 strengthA 深蹲→坐姿腿弯举；strengthB 分腿蹲→自重臀桥），planExercises 记录 exerciseId=替代动作并带 autoSub 标记；计划页/执行页/动作清单均显示替换后动作；替换按钮仍可手动再换（含换回原动作，换回后 autoSub 标记清除）。
- C5 执行页部位轻提示（bodyStatus!=='none' 都弹，膝档同样弹）：动作卡渲染时若动作 notes 非空 → 在动作名/计划行下方显示浅色细条（--accent-soft 底或对应类型浅底），文案 COPY.bodyTips[part]：
  - knee：此动作对膝部负荷较高，如膝部不适请谨慎，可点「替换」换动作。
  - shoulder：此动作对肩部要求较高，肩部有旧伤请谨慎，可点「替换」换动作。
  - back：此动作对腰背要求较高，腰背不适请保持脊柱中立、痛就停。
  细条带 ✕ 关闭按钮，关闭状态存 session（本次训练不再显示）；动作库详情弹层同步显示。提示不阻断。
- C6 膝规则链（红旗 5 项、膝痛≥3 锁定下肢进阶、膝贴士、28 天锁定）全部保留兜底，与 C4/C5 并存。
- C7 有氧器械：bodyStatus==='knee' 时器械选择中「登山机」置灰禁选并提示（改为椭圆仪/跑步机）。「设置」页不新增入口（本轮不做身体情况修改入口）。

## D. 排期确认迁移到计划页
- D1 profile 新字段 scheduleConfirmed:bool。新建档默认 false；旧档案迁移默认 true（老用户不被打扰）。
- D2 触发：进入「计划」页渲染时若 profile.scheduleConfirmed===false → 显示**全屏排期确认视图**（复用 onboarding 排期编辑器组件：标题「我们按你的目标生成了初始排期」+ 一句说明 + 7 天三态 + 恰好 N 天计数）：主按钮「确认排期」（校验恰好 N → scheduleConfirmed=true → 渲染正常计划页）+ 右上「稍后再说」（关闭视图，不置位）。
- D3 红点：scheduleConfirmed===false 期间「计划」Tab 图标右上 6px 抹茶绿/红小圆点（无数字）；确认后消失。「稍后再说」不清红点。
- D4 计划页原「调整每周排期」编辑入口（含天数切换 2-5、重生成 confirm、恰好 N 校验）保留不动；改天数保存后 scheduleConfirmed 视为已确认（置 true）。
- D5 今天页逻辑不受 scheduleConfirmed 影响（默认课表始终可用）。

## E. 验证与交付（本轮）
- node --check src/*.js；python3 build.py；node 冒烟：三档 × 模板解析（knee 档自动替换且不重复）、suggestSchedule 不回归、scheduleConfirmed 状态机、bodyNotes 迁移映射。
- sw.js CACHE 版本号 +1。
- 重启 8765 预览并 present。
- **不做**：PRD_v2.0 源稿/docx 同步、Cloudflare 部署（用户预览确认后另行执行）。

---

# V1.4 改造定稿（2026-09-08 用户确认；优先级高于上文冲突项）

执行范围（沿用 V1.3 约定）：**只做代码修改（A→F）+ 验证看效果；PRD/docx 同步与 Cloudflare 部署延后，待用户预览确认后另行执行。**

## 需求全景
- ① 部位标签**全量化**：EXERCISES 全部 20 个动作标注部位（3 个有氧器械不标）；标签集 knee 膝 / back 腰背 / shoulder 肩 / hip 髋 / core 核心 / wrist 腕 / ankle 踝 / neck 颈 / spine 胸椎（spine 用于胸椎旋转/伸展）。标签=固有属性、**全用户可见**，展示于计划页动作清单、动作库列表与详情、执行页动作卡。
- ② 执行页动作卡「换动作」（合并原「替换」按钮）：弹层上区=推荐替代（SUBSTITUTES 原逻辑）；下区=自己挑一个（**同 target 全库挑选器**，排除自身与当日已含动作，行内显示部位标签+器械类型）。
- ③ 手动换动作**仅当次 session 生效**（R1）：沿用 substituted/substitutedTo，不写模板、不记忆（无"记住"开关）。
- ④ 拍器械识别（路线1）：入口在**动作库页**（不放主 Tab）；BYOK：key 存本地 settings.vision，各用各 key；厂商预设 4 个（智谱 glm-4v-flash 默认 / 通义 / 豆包 / OpenAI）+ 自定义 base_url/model。
- ⑤ 识别调用：OpenAI 兼容 fetch，图片 base64；prompt 内置 20 动作清单，要求返回 {matchId|none, name, target, guide(练哪/发力/注意)}；解析容错（剥 markdown 包裹）。映射=模型端做法 A。
- ⑥ 命中 → 跳动作库该动作详情，注明「仅供参考——识别结果可能并非该器械的精准用法」；详情页显示「加到今天的训练」= 追加到今日力量组**末尾**（今天休息日/无训练则置灰提示）；未命中 → 展示 guide 文字卡 + 「去动作库手动找找」。
- ⑦ 设置页「AI 器械识别」卡：厂商预设+key+自定义（折叠）+清除；拍照前小字「照片将发送给你所选的服务商用于识别」（R5）；无 key 点识别引导去设置（R4）。
- ⑧ db settings 默认 vision:null（向后兼容）；styles 仅新增样式零新色；sw.js CACHE v5。

## 细条边界决策（防噪音）
警示细条（bodyStatus!=='none' 时执行页/动作库详情的可关闭细条）**仅对高负荷部位动作触发**（维持 V1.3 触发集：notes 含 knee/shoulder/back 者——高脚杯深蹲、分腿蹲、上斜卧推、坐姿推胸、上斜肩胛俯卧撑、上斜俯卧撑、绳索后束、罗马尼亚硬拉、坐姿划船、中立握下拉等带这三个标签的动作）；其余新增部位标签（hip/core/wrist/ankle/neck/spine）**只显示标签、不触发细条**。

## 部位标注全量表（authoritative）
高脚杯箱式深蹲 knee｜罗马尼亚硬拉 back｜坐姿划船机 back｜坐姿推胸机 shoulder｜自重臀桥 hip｜死虫 core｜有支撑分腿蹲 knee·hip｜坐姿腿弯举 knee｜上斜哑铃卧推 shoulder·wrist｜中立握高位下拉 shoulder｜绳索后束划船/反向飞鸟 shoulder｜侧桥 core｜上斜肩胛俯卧撑 shoulder｜上斜俯卧撑 shoulder·wrist｜自重提踵 ankle｜徒手髋铰链 hip·back｜胸椎旋转 spine｜胸椎伸展 spine｜靠墙滑手 shoulder｜仰卧/靠墙下巴回收 neck｜跑步机/椭圆仪/登山机 无。

## 技术风险（开工第一步验证，先于 UI 开发）
纯前端浏览器直连厂商 API 的 CORS 放行情况——智谱 open.bigmodel.cn / 通义 dashscope.aliyuncs.com / 豆包 ark.cn-beijing.volces.com / OpenAI api.openai.com。验证方式：curl OPTIONS 预检模拟（Origin: http://127.0.0.1:8765，Access-Control-Request-Headers: authorization,content-type）。

**验证结论（2026-09-08 实测）：四家全部放行浏览器直连**（智谱/豆包/OpenAI 回显 origin+allow-credentials，通义 allow-origin:*，均允许 POST 与 authorization,content-type 头）。路线1 纯前端可行，**厂商预设 4 家全部保留**。

---

# V1.5 改造定稿（2026-09-09 用户确认；优先级高于上文冲突项）

执行范围（沿用 V1.3 约定）：先代码修改 + 预览实测确认；**用户实测通过后（2026-09-09）本次同步 SPEC/PRD/docx 并更新 Cloudflare 部署**。涉及 4 个前端文件：app.js / data.js / engine.js / styles.css / db.js + sw.js CACHE v7。

## ① 分享卡 0/5 区块 → D 方案（app.js drawShareCard，Canvas 1080×1350）
- 完成天数大数字 `200px → 150px`（色 #5A8F4A 不变，基线 y=445）。
- 「本周完成训练」下移与数字拉开（y=532）；数字区与连续天数之间加**浅色分隔线**（#E3DCCB，y=578，居中留白）。
- 「🔥 连续完成 N 天」独立成第三段（y=662，深紫 #6F5F9E）。
- ⭐力量进步区自 y=792 起、亮点行距 74，鼓励语/底部整段纵向重排，确保 1350 高度不溢出。

## ② 分享卡金句 → A 方案（根因：画力量区后 textAlign 停在 left）
- 新增 `wrapCanvasCenter(ctx, text, cx, maxW, lineH, startY)` 居中换行绘制。
- 金句 42px → **48px**（600 字重、色 #2E2A21）、**分行逐行居中**。
- 句子下方加**抹茶绿短细线**（#7FB069，宽度取最长行 ≤560px）。
- 纵位 `y = max(y + 92, 1012)`；底部 footer 改 `data.footer || COPY.shareFooter`。

## ③ 品牌署名 → 设置页 + 三模板（客户名自定义）
- data.js 新增 `BRAND_TEMPLATES`：`{name}的健身管家` / `{name}的私人助教` / `{name}的陪练搭子`（{name}=客户名占位）。
- db.js settings 默认兜底 `shareBrandName:''`、`shareBrandTemplate:0`。
- 新增 `shareFooterFor(name, tplIdx)`：客户名空 → 回退「个人健身训练工作台」（老数据零影响）；超 26 字截断防溢出。
- 设置页新增「分享卡署名」卡：客户名输入框 + 三模板单选（brandCardHtml + sb-save 保存后重渲染）。
- 分享卡生成按钮改 async，传渲染后 footer 给 drawShareCard。

## ④ RIR 每天首次提醒 + 全量双写「剩余次数 (RIR)」
- **拆永久记忆**：maybeShowRirHelp 由 settings.rirHelpShown（永久）改为 **rirHelpDate**（记上次提醒日期）→ 当天已提醒不弹、**跨天重置再弹**；db 兜底 rirHelpDate:null。
- data.js rirHelp 文案重写（**删除旧句**「第一次用后会记住，不用每次看」——与新行为矛盾）。
- UI 全量双写（保留 RIR 术语同时给白话前缀）共 6 处：执行页组记录 RIR 下拉占位「剩余次数(RIR)」、计划表目标行「目标 剩余次数(RIR)」、首次校准小字、阶段表第 1–2 周文案（data.js）等。
- engine.js 建议文案白话化：「RIR≈0/≥2/≥3」→「剩余次数≈0/≥2/≥3」+ 中文解释句。

## ⑤ 自定义有氧项目（四固定预设 + 主有氧；无自由命名入口）
- data.js 新增 `CUSTOM_CARDIO_PRESETS`：**户外 / 游泳 / 骑行 / 球类**（4 固定预设，无细化、无自由命名输入）。
- db.js settings 默认 `customCardio:[]`、`mainCardio:null`。
- 设置页新增「我的有氧项目」卡：四张选项卡点选=激活/取消（co-save）；激活项可设「主有氧」单选；**内置器械三项（跑步机快走/椭圆仪/登山机）固定存在不可动**；主有氧被取消激活自动回退 null。
- **主有氧生效**：设置后 → 有氧日（周二/周四）默认卡片直接显示该项目，替换周二椭圆仪/周四跑步机交替；未设 → 维持现状。
- 执行页有氧卡：下拉 = 内置3（optgroup 健身房器械）+ 已激活预设（optgroup 我的有氧，未激活不出现）；主有氧为默认选中。
- **记录字段（Q4A）**：选中自定义预设项 → 只显示「持续时间 + 主观强度」，隐藏速度/坡度阻力（cardio-extra 区）。
- **knee 档（Q3A）**：选中自定义预设轻提示 `COPY.customKneeHint`「膝部有伤：部分有氧冲击较大，请量力而行」**不拦截**；内置登山机置灰逻辑不变。
- **名称解析统一 `cardioNameOf()`**：周报（app.js:1126 区域）与 CSV 导出全部走它——预设 id → 常量名（永远可解析），内置 id → EXERCISES 名，兜底显示原字符串 → **历史记录取消激活后不丢名**（比 name 快照方案更简，无需冗余存储）。

## ⑥ 三套皮肤切换（默认「奶油」+ 新增「莓果/暗夜」；分享卡不随皮肤）
- **架构**：样式已 token 化 → 仅需 `body[data-theme="berry"|"night"]` 覆盖 `:root` 同名 CSS 变量 + 收敛散落裸色；全站（含弹窗/计划/进度/设置）自动跟随，不动 HTML 结构。
- 新增 token：`--btn-text`（按钮字色）、`--danger-bg`、`--warn-bg`、`--warn-ink`（警示浅底收敛，替换 5 处裸色：styles.css 原 #fff 按钮字、.badge.red/orange 浅底、.alert-red/orange/soft 浅底与 #7C2D12）。
- 三套 token 值：
  | token | 奶油（默认） | 莓果 berry | 暗夜 night |
  |---|---|---|---|
  | 页面底 --bg | #F5F0E6 米 | #FAF0F5 浅藕 | #1B221D 墨绿黑 |
  | 卡片 --card | #FCFAF3 | #FFFBFE | #242E27 |
  | 正文 --ink | #3A362C 暖墨 | #542E43 紫褐 | #EDF1EA 暖白 |
  | 主色 --main | #7FB069 抹茶 | #C4457A 莓果 | #A6C94E 青柠 |
  | 强调 --accent | #6F5F9E 深紫 | #B0872F 香槟金 | #E0B04F 琥珀 |
  | 说明 --ink-sub | #8B8577 | #96798E | #A3AFA2 |
  - 暗夜款按钮字（--btn-text）用深色保证对比度；cardio/posture/rest 类型浅底、warn/danger 警示色全量按主题校准（深色款正文对比度单独把关）。
- app.js：设置页新增「外观」卡（themeCardHtml，三选 radio 带预览点）→ 选中即时 `saveSettings + body.dataset.theme` 切换；boot() 启动应用已存 theme；默认奶油（settings.theme 缺失兜底），老数据零影响。
- **分享卡不随皮肤**（Q2A）：Canvas 保持通用米色品牌物统一。

## 收尾（V1.5，2026-09-09 完成）
- sw.js CACHE **v6 → v7**；重新 build（160,082B）；marker 冒烟全过（data-theme berry/night、剩余次数(RIR)、三品牌模板、户外/游泳/骑行/球类、wrapCanvasCenter、shareFooterFor、mainCardio、customKneeHint、v7）。
- 用户 ?reset=1 全流程实测**通过**（2026-09-09）→ 执行本文档开头「执行范围」中的同步 + 部署。
